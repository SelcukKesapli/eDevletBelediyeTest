<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Appenders>
        <!-- Konsol -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} %-5p [%t] %c{1} - %m%n"/>
        </Console>

        <!-- Dönen dosya (günlük) -->
        <RollingFile name="File"
                     fileName="logs/app.log"
                     filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5p %c{1.}:%L - %m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- AllureMemory Appender -->
        <AllureMemory name="AllureMemory">
            <PatternLayout pattern="[%d{HH:mm:ss}] %-5p %c{1} - %m%n"/>
        </AllureMemory>

    </Appenders>

    <Loggers>
        <!-- Paket bazlı seviyeler -->
        <Logger name="org.openqa.selenium" level="INFO"/>
        <Logger name="io.cucumber" level="INFO"/>

        <!-- Kök logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <AppenderRef ref="AllureMemory"/>
        </Root>
    </Loggers>
</Configuration>
